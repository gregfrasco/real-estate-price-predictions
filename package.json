{
  "name": "real-estate-price-predictions",
  "version": "1.0.0",
  "description": "This package.json file is for heroku deployment only",
  "scripts": {
    "gunicorn": "gunicorn --chdir server app:app",
    "build": "cd client && npm install && npm run build",
    "postinstall": "pip install -r requirements.txt"
  },
  "engines": {
    "node": "0.12"
  },
  "dependencies": {},
  "cacheDirectories": [
    "node_modules",
    "react/node_modules"
  ]
}
