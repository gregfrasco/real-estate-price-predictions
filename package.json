{
  "name": "real-estate-price-predictions",
  "version": "1.0.0",
  "description": "This package.json file is for heroku deployment only",
  "scripts": {
    "gunicorn": "gunicorn --chdir server app:app",
    "build": "cd client && npm run build && npm run cleanup",
    "postinstall": "npm install --prefix client"
  },
  "engines": {
    "node": "12.x"
  },
  "dependencies": {},
  "cacheDirectories": [
    "node_modules",
    "client/node_modules"
  ]
}
